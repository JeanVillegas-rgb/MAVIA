# Known gaps

Status of the loose ends around the adaptive-learning + course-review work.

## Closed in this change

- **Scoring engine ported.** Bayesian Knowledge Tracing now lives in
  `backend/adaptive/services.py` (mavia's own app), not only in the
  `Milestone1-Jean` branch. `adaptive_config` finally has an engine behind its
  knobs. Adapted to `lessons.Question` (linear question order) instead of the
  Jean branch's Bloom-tier `GeneratedQuestion`.
- **Student read API + mobile wiring.** New `GET /api/adaptive/my-courses/`,
  `.../my-courses/<id>/lessons/`, `.../lessons/<id>/`, plus `start/` and
  `submit-response/`. `mobile-app/src/data/library.ts` now calls them, so the
  student course/lesson lists are no longer hard-coded empties.
- **Web/mobile split.** The audiobook-style player is **mobile-only**. The web
  app's Review section only *inspects* the packaged lesson content (tracks +
  questions, read-only) and shows per-enrolled-student progress. There is no
  player, transport, or "mark reviewed" on the web.
- **`mavia/` is a git repo** with a root `.gitignore`.
- **Mobile requests fail fast** (10s `AbortController` timeout) instead of
  hanging a screen on its loading state when the backend is unreachable.

## Still open

### 1. Real audiobook player on mobile — BUILT (needs a dev-client rebuild)
`mobile-app/app/(student)/home/[courseId]/[lessonId].tsx` is now the real
player:
- `src/hooks/useAudioPlayer.ts` wraps `expo-av`'s `Audio.Sound` (load / play /
  pause / seek 15s / position + duration, auto-advance on finish).
- Flattens `GET /api/adaptive/lessons/<id>/` tracks into a playlist with
  transport + a tappable track list; tracks with no generated audio are shown
  disabled.
- After the audio, if the lesson `has_questions` it runs
  `src/components/QuestionCard.tsx` and POSTs `/api/adaptive/submit-response/`;
  otherwise it shows a **review-only** "Lesson reviewed" screen.
- `POST /api/adaptive/start/` is called on entry to get / resume the
  `LearningState`; if the student isn't enrolled it degrades to review-only.

Audio playback uses a **native module** (`expo-audio` on SDK 57 — see §2; was
briefly `expo-av` on SDK 51). Any time that dependency changes, the installed
app must be rebuilt (`npx expo run:android`) or the player screen red-screens
with "Cannot find native module …".

### 2. Expo SDK upgrade (51 → 57) for a real release
Expo Go on the stores now needs a newer SDK than this project's 51, so the app
is dev-client only and can't be handed to a phone via Expo Go.

**Done — builds; pending device smoke-test (2026-09-07).**

Installed: `expo@57.0.20`, `react@19.2.3`, `react-native@0.86.3`,
`expo-audio@57.0.4`, `expo-splash-screen@57.0.8`, `expo-asset`. `tsc --noEmit`
clean; `android/app:assembleDebug` → **BUILD SUCCESSFUL** (`app-debug.apk`).

Changes made during the migration:
- `mobile-app/.npmrc` → `legacy-peer-deps=true` (npm won't co-install React 19
  + the transitional tree without it).
- `app.json` → top-level `splash` removed (invalid in SDK 57). Do **not** add
  the `expo-splash-screen` plugin without a splash image — it emits a
  `@drawable/splashscreen_logo` reference and `aapt2` fails to link.
- `tsconfig.json` → `"ignoreDeprecations": "6.0"` (TS 6 flags `baseUrl`).
- `app/_layout.tsx` → `<StatusBar backgroundColor>` removed (prop gone in
  `expo-status-bar` 57; Android is edge-to-edge).
- `expo-av` → **`expo-audio`**: `src/hooks/useAudioPlayer.ts` rewritten
  (`createAudioPlayer(src, { updateInterval })`, `addListener(
  "playbackStatusUpdate")`, `seekTo(seconds)`). External contract unchanged
  (millis, `load/play/pause/seek`) so `[lessonId].tsx` didn't change.
- `android/` regenerated by `prebuild --clean` — commit the new folder.

**Remaining:** on the device — `npx expo run:android` (or `adb install -r
android/app/build/outputs/apk/debug/app-debug.apk`), then test login → course
list → lesson player: audio playback via `expo-audio`, transport + auto-advance,
question submit, review-only screen. Watch the first play for any `expo-audio`
runtime surprise (the hook is type-correct but hasn't run on a device yet).

### 3. Standalone build
No EAS/standalone build has been produced. For a shareable APK:

```
cd mobile-app
npm i -g eas-cli
eas login
eas build:configure
eas build -p android --profile preview   # needs an Expo account
```

Local alternative without EAS: `npx expo prebuild && cd android && ./gradlew assembleRelease`.

### 4. Phone-over-Wi-Fi networking
Tonight's device run used USB + `adb reverse tcp:8000 tcp:8000`. For Wi-Fi:

- Set the Windows network profile for the Wi-Fi adapter to **Private**
  (`Get-NetConnectionProfile`; `Set-NetConnectionProfile -InterfaceAlias "Wi-Fi" -NetworkCategory Private`).
- Allow inbound 8000/5173 (run elevated):
  ```
  netsh advfirewall firewall add rule name="MAVIA dev 8000" dir=in action=allow protocol=TCP localport=8000
  netsh advfirewall firewall add rule name="MAVIA dev 5173" dir=in action=allow protocol=TCP localport=5173
  ```
- Run Django on the LAN: `python manage.py runserver 0.0.0.0:8000`.
- On the phone, set `EXPO_PUBLIC_API_BASE_URL=http://<machine-LAN-IP>:8000/api`
  in `mobile-app/.env`.

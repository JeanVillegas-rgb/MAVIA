Django>=5.0,<6.0
djangorestframework>=3.15
django-cors-headers>=4.3
python-dotenv>=1.0

# lessons (content generation): PDF text/image extraction, TF-IDF outline
# matching, and edge-tts audio. Deterministic except for the optional figure
# describer, which calls a local Ollama vision model over HTTP (requests) and
# degrades to captions when Ollama isn't running.
PyMuPDF>=1.24
scikit-learn>=1.4
edge-tts>=7.0
requests>=2.31
